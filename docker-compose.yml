version: '3.8'

services:
  app:
    build: .
    container_name: kigali-estates
    ports:
      - "3000:3000"
    volumes:
      - db_data:/app/data
    restart: unless-stopped
    environment:
      - PORT=3000
      - NODE_ENV=production

volumes:
  db_data:
